# 进度文档

## 2026-06-06

状态：已完成

本次变更：收敛项目主入口，删除 `eleven-rag` 旧副本，仅保留 `eleven-agent-platform` 作为后端服务、脚本、测试和文档引用的唯一主入口。

实现方式变化：仓库内不再维护 `rag_system/RAGSystem` 旧入口；现有 README 与脚本继续统一指向 `eleven-agent-platform`。

当前限制：本次只做入口收敛，未处理既有 `.env` 跟踪、CI 占位配置、`.pytest_cache` 权限和未跟踪 `__pycache__` 等工程化问题。

下一步建议：补充 `.gitignore`，清理敏感配置跟踪，并将 CI 调整为可执行的 `pytest eleven-agent-platform/tests`。

## 2026-06-06 README 同步

状态：已完成

本次变更：同步 README 中的项目入口、目录结构、持久化方案和生产替换点描述。

实现方式变化：README 明确 `eleven-agent-platform` 是唯一后端主入口，并将过时的 SQLite/待升级 FAISS 表述改为当前 MySQL + Redis + FAISS 的实际状态。

当前限制：本次仅同步 README，不处理 `.env` 跟踪、CI 占位配置、`.pytest_cache` 权限和未跟踪 `__pycache__`。

下一步建议：继续补齐 `.gitignore` 与可执行 CI，减少测试后生成文件对 Git 状态的干扰。

## 2026-06-06 工程遗留处理

状态：已完成

本次变更：补充 `.gitignore` 与 `.gitattributes`，关闭 pytest cacheprovider，替换占位 CI，并将 `.env`、`.rag_store`、`.run`、`tmp_meta.db` 从 Git 跟踪中移除但保留本地文件。

实现方式变化：测试默认入口收敛为 `eleven-agent-platform/tests`；Git 统一按 `.gitattributes` 约束文本换行；PR/Push CI 改为执行 `uv sync --group dev` 和 `pytest`。

当前限制：本地 `.env` 仍需开发者自行维护，已不建议进入版本控制；历史提交中如已包含敏感信息，仍需额外做密钥轮换和历史清理。

下一步建议：如后续继续生产化，补充 Docker/compose、本地 MySQL/Redis 启动说明，以及真实集成测试数据隔离策略。

## 2026-06-06 项目分析待优化项

状态：未完成

本次变更：记录项目分析中识别出的主要短板与优先优化方向，作为后续生产化和简历亮点补齐依据。

实现方式变化：暂无代码实现变化，本次仅同步待优化事项到进度文档。

当前限制：
- 检索重排仍偏 MVP：当前是 FAISS 召回叠加简化 lexical/token 打分，不是真 BM25，也没有独立 reranker。
- FAISS 当前是本地索引文件，不是生产级独立向量库；README 中也已将其作为后续生产替换点。
- LLM 调用只有基础 HTTP 客户端，缺少系统化超时、重试、降级、限流和响应修复闭环。
- 记忆系统目前只有偏好记忆和短时会话，距离“偏好记忆 / 知识记忆 / 会话记忆”的完整治理还差编辑、删除、追溯、权限和一致性。
- 缺少 Docker/Compose，本地依赖 MySQL、Redis、模型缓存，项目交付和复现门槛偏高。

下一步建议：
1. 补充 Docker Compose：提供 MySQL、Redis、API 一键启动能力，降低本地复现成本。
2. 将检索升级为真正的 BM25 + FAISS + reranker 混合召回/重排方案，并将权重配置化。
3. 完善 memory 的删除、编辑、审计、索引一致性，补齐记忆治理闭环。
4. 给 LLM 调用补充超时重试、降级策略、结构化响应修复和失败可观测能力。
5. 将 RAG Eval 接入 CI，设置最小阈值，形成检索和引用质量门禁。

## 2026-06-06 检索升级为真实混合召回

状态：已完成

本次变更：将检索主链从“FAISS + 简化 lexical/token 打分”升级为真正的 `BM25 + FAISS + CrossEncoder reranker` 混合召回/重排方案，并把召回路数、候选池大小、BM25 参数和融合权重全部配置化。

实现方式变化：新增 `qa/retrieval_stack.py`，用 Okapi BM25 公式做关键词召回，用 FAISS 做向量召回，用 `sentence-transformers` 的 `CrossEncoder` 做真实 reranker，再按 `HYBRID_BM25_WEIGHT/HYBRID_VECTOR_WEIGHT/HYBRID_RERANKER_WEIGHT` 融合最终分数；同时补充了检索回归测试。

当前限制：
- BM25 当前仍是基于 MySQL `chunks` 全量拉取后的进程内计算，召回质量是真 BM25，但规模能力还不是生产级全文索引。
- reranker 依赖 `BAAI/bge-reranker-v2-m3` 模型可下载或已缓存；首次运行若本地没有模型，会有预热/下载成本。

下一步建议：
1. 将 BM25 从进程内实现替换为独立全文检索能力（如 ES / OpenSearch / Tantivy 系方案），避免文档规模上来后每次全量扫描。
2. 给 reranker 增加预热脚本、健康检查和超时降级策略，避免模型首次加载拖慢首个请求。

## 2026-06-06 Safety / Alignment 第一阶段工程化

状态：已完成

本次变更：按 `input_guard -> output_guard -> risk_level/降级 -> 权限过滤 -> 安全评测集 -> 审计日志` 顺序，把安全与可控能力接入了问答主链。

实现方式变化：
- 新增 `guards/input_guard.py`，对提示词注入、敏感信息外泄、危险请求做输入拦截和风险分级。
- 新增 `guards/output_guard.py`，对无引用、伪造引用、敏感信息泄露做输出校验，不通过时自动降级成“证据式回答”。
- 在 `qa/answering.py` 中加入 `risk_level` 驱动的降级策略：高风险问题默认不走自由生成，只返回可引用证据摘要。
- 新增 `authz/access_control.py`，基于 `USER_DOC_PERMISSIONS` 做用户文档前缀权限过滤，并在 `/v1/chat` 支持传入 `doc_id_prefixes` 进一步收窄作用域。
- 新增 `audit/audit_logger.py`，将风险等级、权限决策、检索结果、输出降级等信息落为本地 JSONL 审计日志。
- 扩展 `evaluation` 数据集和指标，新增 `expected_refusal / required_citations / forbidden_chunk_ids / allowed_doc_prefixes`，并统计 `refusal_match_rate / citation_validity_rate / forbidden_chunk_leak_rate`。

当前限制：
- 输入审查当前仍以规则匹配为主，适合拦截明显风险请求，但对隐晦攻击和复杂越权语义还不够强。
- 权限过滤目前是“用户 -> 文档前缀”级别，不是完整的租户、角色、资源三级授权模型。
- 审计日志当前写本地 JSONL，适合开发和演示，不适合高并发生产观测。

下一步建议：
1. 将规则式 input guard 升级为“规则 + 分类模型”双层检测，降低绕过率和误杀率。
2. 将权限模型升级到 chunk 级 metadata ACL，并补充管理端配置入口。
3. 将审计日志接到集中式观测链路，并增加风险告警和追踪检索。
