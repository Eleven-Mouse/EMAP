# 进度文档

## 2026-06-07 Docker 环境变量边界收紧

状态：已完成
本次变更：移除 `docker-compose.yml` 中 `api` 服务对本机 `.env` 的直接 `env_file` 注入，改为可选读取 `.env.docker`；新增 `.env.docker.example` 作为容器专用覆盖模板，并在 `.gitignore` 中忽略 `.env.docker`，同时更新 `README.md` 的 Docker 启动说明。
实现方式变化：现在容器默认只吃 Compose 明确白名单注入的运行时配置，本机 `uv` 路径继续使用 `.env`；如需给容器传入 LLM 或检索覆盖项，需显式复制 `.env.docker.example` 为 `.env.docker` 再启动 Compose。
当前限制变化：容器环境与本机开发环境的边界已经清晰很多，但 Docker Compose 仍会读取根目录 `.env` 做变量替换语义；当前因为 Compose 文件不再使用 `${...}` 引用，本机 `.env` 不会再被顺手注入容器，但后续若新增变量插值，仍需注意这一点。
下一步建议：
1. 手动检查本机 `.env` 的第 1 行格式，当前它看起来把 `APP_ENV=dev` 串进了 `APP_NAME`，虽不再影响容器注入，但仍会影响本机 `uv` 路径的配置读取。
2. 若后续继续完善容器交付，可为 Docker 单独补一份 `.env.docker.local.md` 说明，列清楚哪些键允许容器覆盖、哪些键应保持 Compose 固定。

## 2026-06-07 Docker / Compose 本地交付骨架

状态：已完成
本次变更：新增 `Dockerfile`、`docker-compose.yml`、`.dockerignore` 与 `scripts/docker-entrypoint.sh`，补齐本地一键拉起 `api + mysql + redis` 的最小容器化方案；同时更新 `README.md`，新增 Docker Compose 启动、日志查看、停止方式和已知限制说明。
实现方式变化：容器内通过 `docker-entrypoint.sh` 先等待 MySQL / Redis 就绪，再自动执行 `memory schema` 迁移，最后启动 FastAPI；`docker-compose.yml` 中显式覆盖容器内的 `MYSQL_DSN`、`REDIS_URL`、`RAG_STORE_DIR` 等配置，避免直接复用本机 `.env` 时把 `127.0.0.1` 误指向容器自身。
当前限制变化：项目现在已经具备基础本地容器交付能力，但这套 Compose 仍以“快速复现开发环境”为目标，不是生产部署模板；embedding / reranker 首次下载较慢，复杂 PDF 解析也仍可能需要额外系统依赖调试。
下一步建议：
1. 若后续继续增强交付性，可为 Compose 增加专用 `.env.docker` 或 profile，把开发态和容器态配置进一步分开。
2. 若要把真实依赖集成测试接入 CI，可复用这套 Compose 环境，在单独 job 中拉起 MySQL / Redis 后执行 `pytest -m integration`。

## 2026-06-07 BACKEND_GUIDE 对齐重构

状态：已完成
本次变更：重构 `BACKEND_GUIDE.md`，将原先偏“理想分层设计说明”的内容改为基于当前仓库真实实现的后端导读，重点对齐了 README 的新口径，明确说明当前主链路、`qa/answering.py` 的真实职责、`FaissVectorStore` 命名、三类测试分层，以及哪些能力仍然只是后续演进方向。
实现方式变化：本次未修改业务代码，只重写导读结构，让文档从“应该怎么设计”转为“现在这份仓库该怎么读、先看哪里、哪些地方不能误读成已完成”。
当前限制变化：README 与 BACKEND_GUIDE 的事实边界已经基本一致，但后续如果继续调整问答编排、记忆治理或测试体系，仍需同步维护两份文档，避免再次出现“一个偏现实、一个偏理想”的信息漂移。
下一步建议：
1. 若后续继续增强项目交付性，可补一份面向协作者的 `docs/` 目录规划说明，把 README、BACKEND_GUIDE、导学、面经的定位再明确分层。
2. 若未来真的引入 learning 域或独立向量库，实现落地后应优先更新 BACKEND_GUIDE 的“当前真实链路”章节，而不是只改 README。

## 2026-06-07 README 重构

状态：已完成
本次变更：重构根目录 `README.md`，从原来的“信息齐全但偏堆料”改为更适合交付和面试的结构，重点补清了项目定位、当前状态、技术栈、核心能力、启动方式、常用命令、API 概览、默认测试与真实依赖集成测试说明，以及当前实现边界。
实现方式变化：本次未修改业务代码，只重组文档结构并对齐最近实际实现，包括 `FaissVectorStore` 命名、E2E 测试、`EMAP_RUN_INTEGRATION_TESTS=1` 的 gated integration tests 方案和当前 MVP 边界。
当前限制变化：README 已更适合新同学快速上手和面试叙述，但 `BACKEND_GUIDE.md` 仍更偏“导学视角”；两份文档目前是互补关系，后续若架构继续调整，需要同步维护两边的一致性。
下一步建议：
1. 若后续补 Docker Compose，同步把 README 的启动章节升级为“一键本地环境 + 一键测试”版本。
2. 若将真实依赖集成测试接入 CI，README 中的测试章节应继续补充 CI job 说明与失败排查提示。

## 2026-06-07 真实依赖集成测试门禁

状态：已完成
本次变更：新增 `eleven-agent-platform/tests/test_live_integrations.py`，补齐三类真实依赖集成测试用例：MySQL 元数据 `replace_chunks` 替换一致性、MySQL + Redis 组合下的偏好/会话记忆写读、以及基于真实 FAISS 索引文件的“索引 -> 删除 -> 重建 -> 检索”闭环；同时在 `pyproject.toml` 注册 `integration` marker。
实现方式变化：集成测试默认通过 `EMAP_RUN_INTEGRATION_TESTS=1` 显式开启，未开启时模块级直接跳过；FAISS 集成测试走真实索引文件持久化，但使用测试向量桩替代真实 embedding 模型，避免测试时下载模型或依赖外网。
当前限制变化：现在项目已经具备“默认单测稳定运行 + 手动开启真实依赖验真”的双层测试结构，但集成测试尚未接入 CI，也还没覆盖真实 `AgentSystem` 全链路联调。
下一步建议：
1. 在 README 或 BACKEND_GUIDE 中补一段集成测试执行说明，明确 `EMAP_RUN_INTEGRATION_TESTS=1` 的使用方式和前置依赖。
2. 后续若补 Docker Compose，可把 MySQL/Redis 测试环境容器化，再将集成测试并入 CI 的单独 job。

## 2026-06-07 问答编排重构与 E2E 补测

状态：已完成
本次变更：重构 `eleven-agent-platform/qa/answering.py`，将原本集中在 `IntelligentQA` 中的“混合检索编排”和“回答生成”拆分为 `RetrievalOrchestrator` 与 `AnswerGenerator` 两个协作对象，同时保留 `IntelligentQA.retrieve/ask` 的对外接口不变；新增 `eleven-agent-platform/tests/test_ingest_retrieve_chat_e2e.py`，补齐 `ingest -> retrieve -> chat` 以及“重复导入替换旧 chunk/索引”的端到端流程测试。
实现方式变化：本次没有引入新的框架或额外抽象层，而是在原文件内做外科手术式拆分，让 `IntelligentQA` 继续充当门面；新增的 E2E 测试使用共享的内存版 metadata/vector 仓储，保证导入、检索、问答三段逻辑确实跑在同一份状态上，而不是彼此隔离的假测试。
当前限制变化：`answering.py` 的可维护性明显提升，但审计、guard、权限、LLM 降级仍然由 `IntelligentQA` 统一编排，后续若继续扩功能，仍建议把 trace/guard 流程进一步抽成独立步骤；E2E 测试当前覆盖的是内存假仓储闭环，还没覆盖真实 MySQL/Redis/FAISS 依赖的集成环境。
下一步建议：
1. 继续补一组真实依赖集成测试，重点覆盖 MySQL 元数据替换、Redis 会话追加和 FAISS 重建后的检索一致性。
2. 基于现有 `evaluation/runner.py` 固定一套可重复跑的基准集，把召回率、引用有效率、安全指标落到 README 和简历话术里。

## 2026-06-07 向量存储命名对齐

状态：已完成
本次变更：将 `eleven-agent-platform/vector_storage/vector_store.py` 中与实际实现不符的 `QdrantVectorStore` 更名为 `FaissVectorStore`，并同步更新 `agent_system/facade.py` 中的导入、类型标注与实例化调用，消除“FAISS 适配层却挂 Qdrant 名字”的命名错位。
实现方式变化：本次仅调整类名与引用，不改动底层 `vector_repository` 的行为，实现仍然是当前项目使用的本地 FAISS 向量检索链路。
当前限制变化：命名已经与实现保持一致，但向量存储适配层仍然只有 FAISS 这一种实现；如果后续真的要引入独立向量库，再补真正的多实现适配会更合理。
下一步建议：
1. 若后续要支持独立向量库，新增明确的适配接口，再分别落地 `FaissVectorStore` 与真实远端向量库实现。
2. 配合真实依赖集成测试，验证向量索引重建、删除和检索结果在 FAISS 实现下的一致性。

## 2026-06-07 面经与导学补充

状态：已完成
本次变更：基于当前仓库中的 README、后端导读、核心代码、测试与 CI，新增 `导学-EMAP.md` 与 `面经-EMAP.md` 两份项目分析文档，补齐学习路径、亮点拆解、相对路径阅读指引、第一人称 STAR 面经口播与量化验证建议。
实现方式变化：本次没有修改业务代码，主要把现有实现沉淀成可复用的面试材料；文档内容明确区分“当前已落地能力”和“仍处于（待测）或后续演进”的边界，避免把 MVP 说成生产终态。
当前限制变化：新增文档已覆盖混合检索、引用治理、安全降级、权限过滤、记忆存储和评测闭环，但真实业务量化数据仍需后续用 `eval/` 数据集和评测脚本补采，文档中已统一标注 `（待测）` 并给出测法。
下一步建议：
1. 用真实业务语料补齐 `eval/` 样本，跑出可落简历的数据指标。
2. 若要继续强化项目含金量，优先补知识记忆编辑/删除闭环、Docker Compose 和更生产化的全文检索/向量存储方案。

## 2026-06-06

状态：已完成

本次变更：收敛项目主入口，删除 `eleven-rag` 旧副本，仅保留 `eleven-agent-platform` 作为后端服务、脚本、测试和文档引用的唯一主入口。

实现方式变化：仓库内不再维护 `rag_system/RAGSystem` 旧入口；现有 README 与脚本继续统一指向 `eleven-agent-platform`。

当前限制：本次只做入口收敛，未处理既有 `.env` 跟踪、CI 占位配置、`.pytest_cache` 权限和未跟踪 `__pycache__` 等工程化问题。

下一步建议：补充 `.gitignore`，清理敏感配置跟踪，并将 CI 调整为可执行的 `pytest eleven-agent-platform/tests`。

## 2026-06-06 README 同步

状态：已完成

本次变更：同步 README 中的项目入口、目录结构、持久化方案和生产替换点描述。

实现方式变化：README 明确 `eleven-agent-platform` 是唯一后端主入口，并将过时的 SQLite/待升级 FAISS 表述改为当前 MySQL + Redis + FAISS 的实际状态。

当前限制：本次仅同步 README，不处理 `.env` 跟踪、CI 占位配置、`.pytest_cache` 权限和未跟踪 `__pycache__`。

下一步建议：继续补齐 `.gitignore` 与可执行 CI，减少测试后生成文件对 Git 状态的干扰。

## 2026-06-06 工程遗留处理

状态：已完成

本次变更：补充 `.gitignore` 与 `.gitattributes`，关闭 pytest cacheprovider，替换占位 CI，并将 `.env`、`.rag_store`、`.run`、`tmp_meta.db` 从 Git 跟踪中移除但保留本地文件。

实现方式变化：测试默认入口收敛为 `eleven-agent-platform/tests`；Git 统一按 `.gitattributes` 约束文本换行；PR/Push CI 改为执行 `uv sync --group dev` 和 `pytest`。

当前限制：本地 `.env` 仍需开发者自行维护，已不建议进入版本控制；历史提交中如已包含敏感信息，仍需额外做密钥轮换和历史清理。

下一步建议：如后续继续生产化，补充 Docker/compose、本地 MySQL/Redis 启动说明，以及真实集成测试数据隔离策略。

## 2026-06-06 项目分析待优化项

状态：未完成

本次变更：记录项目分析中识别出的主要短板与优先优化方向，作为后续生产化和简历亮点补齐依据。

实现方式变化：暂无代码实现变化，本次仅同步待优化事项到进度文档。

当前限制：
- 检索重排仍偏 MVP：当前是 FAISS 召回叠加简化 lexical/token 打分，不是真 BM25，也没有独立 reranker。
- FAISS 当前是本地索引文件，不是生产级独立向量库；README 中也已将其作为后续生产替换点。
- LLM 调用只有基础 HTTP 客户端，缺少系统化超时、重试、降级、限流和响应修复闭环。
- 记忆系统目前只有偏好记忆和短时会话，距离“偏好记忆 / 知识记忆 / 会话记忆”的完整治理还差编辑、删除、追溯、权限和一致性。
- 缺少 Docker/Compose，本地依赖 MySQL、Redis、模型缓存，项目交付和复现门槛偏高。

下一步建议：
1. 补充 Docker Compose：提供 MySQL、Redis、API 一键启动能力，降低本地复现成本。
2. 将检索升级为真正的 BM25 + FAISS + reranker 混合召回/重排方案，并将权重配置化。
3. 完善 memory 的删除、编辑、审计、索引一致性，补齐记忆治理闭环。
4. 给 LLM 调用补充超时重试、降级策略、结构化响应修复和失败可观测能力。
5. 将 RAG Eval 接入 CI，设置最小阈值，形成检索和引用质量门禁。

## 2026-06-06 检索升级为真实混合召回

状态：已完成

本次变更：将检索主链从“FAISS + 简化 lexical/token 打分”升级为真正的 `BM25 + FAISS + CrossEncoder reranker` 混合召回/重排方案，并把召回路数、候选池大小、BM25 参数和融合权重全部配置化。

实现方式变化：新增 `qa/retrieval_stack.py`，用 Okapi BM25 公式做关键词召回，用 FAISS 做向量召回，用 `sentence-transformers` 的 `CrossEncoder` 做真实 reranker，再按 `HYBRID_BM25_WEIGHT/HYBRID_VECTOR_WEIGHT/HYBRID_RERANKER_WEIGHT` 融合最终分数；同时补充了检索回归测试。

当前限制：
- BM25 当前仍是基于 MySQL `chunks` 全量拉取后的进程内计算，召回质量是真 BM25，但规模能力还不是生产级全文索引。
- reranker 依赖 `BAAI/bge-reranker-v2-m3` 模型可下载或已缓存；首次运行若本地没有模型，会有预热/下载成本。

下一步建议：
1. 将 BM25 从进程内实现替换为独立全文检索能力（如 ES / OpenSearch / Tantivy 系方案），避免文档规模上来后每次全量扫描。
2. 给 reranker 增加预热脚本、健康检查和超时降级策略，避免模型首次加载拖慢首个请求。

## 2026-06-06 Safety / Alignment 第一阶段工程化

状态：已完成

本次变更：按 `input_guard -> output_guard -> risk_level/降级 -> 权限过滤 -> 安全评测集 -> 审计日志` 顺序，把安全与可控能力接入了问答主链。

实现方式变化：
- 新增 `guards/input_guard.py`，对提示词注入、敏感信息外泄、危险请求做输入拦截和风险分级。
- 新增 `guards/output_guard.py`，对无引用、伪造引用、敏感信息泄露做输出校验，不通过时自动降级成“证据式回答”。
- 在 `qa/answering.py` 中加入 `risk_level` 驱动的降级策略：高风险问题默认不走自由生成，只返回可引用证据摘要。
- 新增 `authz/access_control.py`，基于 `USER_DOC_PERMISSIONS` 做用户文档前缀权限过滤，并在 `/v1/chat` 支持传入 `doc_id_prefixes` 进一步收窄作用域。
- 新增 `audit/audit_logger.py`，将风险等级、权限决策、检索结果、输出降级等信息落为本地 JSONL 审计日志。
- 扩展 `evaluation` 数据集和指标，新增 `expected_refusal / required_citations / forbidden_chunk_ids / allowed_doc_prefixes`，并统计 `refusal_match_rate / citation_validity_rate / forbidden_chunk_leak_rate`。

当前限制：
- 输入审查当前仍以规则匹配为主，适合拦截明显风险请求，但对隐晦攻击和复杂越权语义还不够强。
- 权限过滤目前是“用户 -> 文档前缀”级别，不是完整的租户、角色、资源三级授权模型。
- 审计日志当前写本地 JSONL，适合开发和演示，不适合高并发生产观测。

下一步建议：
1. 将规则式 input guard 升级为“规则 + 分类模型”双层检测，降低绕过率和误杀率。
2. 将权限模型升级到 chunk 级 metadata ACL，并补充管理端配置入口。
3. 将审计日志接到集中式观测链路，并增加风险告警和追踪检索。
## 2026-06-07 后续开发路线图（三阶段）

状态：未完成
本次变更：基于当前仓库已落地的 `FastAPI + MySQL + Redis + FAISS + BM25 + reranker + guard + audit + eval` 骨架，补充一版按仓库真实结构拆分的三阶段开发路线图，作为后续把项目从“记忆增强 RAG 后端 MVP”继续升级为“更完整的记忆系统后端”的执行基线。
实现方式变化：本次不修改业务代码，只把下一步开发拆成 3 周 6 个里程碑，并明确落到现有模块与文件，避免后续优化继续停留在概念层。路线图默认按 `知识记忆治理闭环 -> 独立检索基础设施 -> Eval / 可观测 / 质量门禁` 顺序推进，优先复用 `eleven-agent-platform` 现有分层，而不是另起炉灶重搭一套。
当前限制变化：当前仓库已经具备偏好记忆、会话短时记忆、混合检索、引用回答、安全降级和基础评测能力，但知识记忆生命周期、独立检索后端适配、异步索引链路，以及持续质量门禁仍未真正落地；这也是项目与更完整 memory platform 之间的主要差距。
下一步建议：
1. 第 1 周先补“知识记忆治理闭环”。新增 `eleven-agent-platform/repositories/knowledge_repository.py`、`eleven-agent-platform/schemas/knowledge.py`，扩展 `eleven-agent-platform/services/memory_service.py`、`eleven-agent-platform/controllers/memory_controller.py`、`eleven-agent-platform/services/container.py` 与 `scripts/manage_memory_schema.py`，补齐知识记忆的创建、编辑、软删除、恢复、版本历史、审计事件和检索可见性控制；同时新增 `tests/test_knowledge_repository.py`、`tests/test_knowledge_memory_api.py`、`tests/test_knowledge_memory_retrieval.py`。
2. 第 2 周再做“独立检索基础设施”。围绕 `eleven-agent-platform/repositories/vector_repository.py`、`eleven-agent-platform/vector_storage/`、`eleven-agent-platform/qa/retrieval_stack.py` 和 `eleven-agent-platform/services/container.py` 抽象向量与关键词检索适配层，优先保留现有 FAISS 实现并预留 Qdrant / OpenSearch 入口；同时扩展 `eleven-agent-platform/services/ingestion_service.py`、`eleven-agent-platform/controllers/ingestion_controller.py`、`eleven-agent-platform/document_processing/pipeline.py`，新增 `repositories/index_job_repository.py`、`services/indexing_service.py`、`scripts/run_index_worker.py`，把同步 ingest 拆成带 `pending / processing / ready / failed` 状态的异步索引任务链路，并补 `tests/test_vector_backend_switch.py`、`tests/test_index_job_flow.py`、`tests/test_ingest_async_api.py`。
3. 第 3 周最后固化“Eval / 可观测 / 质量门禁”。继续扩展 `eleven-agent-platform/evaluation/runner.py`、`eleven-agent-platform/qa/answering.py`、`eleven-agent-platform/audit/audit_logger.py`、`eleven-agent-platform/authz/access_control.py`、`eleven-agent-platform/guards/` 和 `.github/workflows/`，把检索候选、重排分数、引用结果、降级原因、权限过滤和删除泄漏验证纳入 trace / audit / CI；同步补充 `eval/` 数据集与 `tests/test_eval_runner.py`、`tests/test_answering_safety.py`、`tests/test_access_control.py`、`tests/test_live_integrations.py`，形成能卡住召回率、引用有效率、拒答命中率和越权泄漏率的最小质量门禁。
## 2026-06-07 知识记忆治理闭环第一阶段

状态：已完成
本次变更：新增知识记忆的完整生命周期能力，落地了 MySQL 迁移脚本 `scripts/init_mysql_knowledge_memory.sql` 和迁移注册 `scripts/manage_memory_schema.py`，新增 `eleven-agent-platform/repositories/knowledge_repository.py`、`eleven-agent-platform/schemas/knowledge.py`，并扩展 `services/memory_service.py`、`agent_system/facade.py`、`controllers/memory_controller.py`、`services/container.py`，补齐知识记忆的创建、编辑、软删除、恢复、历史查询和事件记录；同时把知识记忆接入 `qa/answering.py` 的检索候选源，让它可以在问答链路中作为可引用来源返回。
实现方式变化：知识记忆没有强行塞进现有偏好记忆表，而是独立建模为 `knowledge_memories / knowledge_memory_versions / knowledge_memory_events` 三张表；问答侧保持现有 `BM25 + vector + reranker` 编排不变，只是把知识记忆作为第二类候选源接入，并通过 `SourceItem.source_type / memory_id / scope_id` 保留来源类型和治理信息。为避免环境依赖把测试和非 ingest 场景整条链路拖死，还顺手修复了两个潜在问题：`document_processing/pipeline.py` 改为缺少 splitter 依赖时退化到基础切分实现，`services/container.py` 与 `qa/answering.py` 在知识记忆仓库或 Redis 依赖暂不可用时不再让无关单测直接崩掉。
当前限制变化：当前知识记忆已经支持 CRUD、软删除/恢复、版本历史和问答检索接入，但还没有接入独立向量索引，也还没有做更细粒度的 chunk 级 ACL 与后台管理端；知识记忆当前默认通过 `scope_id` 复用现有前缀权限模型，并主要依赖 BM25 / reranker 参与召回。
下一步建议：
1. 在第二阶段把知识记忆继续接入独立检索基础设施，优先补 `vector_storage/` 适配层与异步索引任务，使知识记忆和文档 chunk 都能挂到统一的可替换检索后端上。
2. 为 `eval/` 补一组知识记忆专用样本，覆盖“编辑后命中新版本”“删除后不可召回”“恢复后重新可见”“越权 scope 不可泄漏”四类场景，并把这些指标接进 CI 门禁。
3. 如需进入更接近生产的形态，再补 `test_live_integrations.py` 对知识记忆表的真实 MySQL 集成测试；当前默认单测和 E2E 已经覆盖核心逻辑，但真实依赖环境仍取决于本地是否安装 `redis` 和 `sqlalchemy`。
## 2026-06-07 第二阶段：独立检索基础设施与异步索引链路

状态：已完成
本次变更：围绕第二阶段目标补齐了两块核心能力。其一，新增 `eleven-agent-platform/vector_storage/base.py`、`faiss_backend.py`、`memory_backend.py`、`qdrant_backend.py`，并重写 `repositories/vector_repository.py` 与 `services/container.py`，把原先写死在仓储里的 FAISS 逻辑抽成可切换后端，当前支持 `faiss`、`memory`，并预留 `qdrant` 适配入口；其二，新增 `scripts/init_mysql_index_jobs.sql`、`repositories/index_job_repository.py`、`schemas/indexing.py`、`services/indexing_service.py`、`scripts/run_index_worker.py`，同时扩展 `services/ingestion_service.py`、`controllers/ingestion_controller.py` 与 `services/memory_service.py`，落地 `document / knowledge_memory` 两类索引任务的 `pending / processing / ready / failed` 状态机，让文档导入和知识记忆索引都可以走统一 job 链路。
实现方式变化：这次没有直接废弃现有同步 `/v1/ingest`，而是保留原链路兼容当前功能，同时新增 `/v1/ingest/jobs` 与 `/v1/ingest/jobs/{job_id}` 作为异步索引入口；默认通过 `INDEX_JOB_AUTOPROCESS=true` 保持“提交即处理”的平滑行为，但任务状态已经完整记录进 `index_jobs` 表，后续可以切成真正的后台 worker 模式。知识记忆写入侧也不再只停留在 CRUD，而是在创建、更新、删除、恢复后自动提交索引任务，使它与文档 chunk 一样能进入统一的向量索引体系。
当前限制变化：第二阶段解决了“检索后端写死单机实现”和“索引过程不可观测”的问题，但当前默认后端仍是 `faiss`；`qdrant` 适配已经留好接口和基本防坑逻辑，不过本地环境未安装 `qdrant-client` 时不会实际启用。异步索引链路目前仍是单进程 worker 语义，适合本地和 MVP，不是分布式任务系统；此外真实依赖测试依旧受本地 `redis`、`sqlalchemy` 包是否安装影响。
下一步建议：
1. 继续把知识记忆和文档索引状态纳入 `health` / audit / eval 维度，补“索引未 ready 时禁止误命中”和“failed job 可重试”相关评测样本。
2. 如果准备进一步接近生产，优先把 `INDEX_JOB_AUTOPROCESS` 关掉，配合 `scripts/run_index_worker.py` 演练真正的异步处理模式，再视情况引入 MQ。
3. 如果准备切远端向量库，下一步先补 `qdrant-client` 依赖、容器化配置和最小集成测试，再考虑把知识记忆和文档 chunk 的索引重建流程统一进同一套运维脚本。
## 2026-06-10 绗簩杞仴搴锋鏌ヤ笌鏃ュ織瀛楁鍥哄寲

鐘舵€侊細閮ㄥ垎瀹屾垚
鏈鍙樻洿锛氬湪绗竴杞殑缁撴瀯鍖栨棩蹇楀拰鍩虹 `/health` 涔嬩笂锛岀户缁悜鈥滄洿鍍忎紒涓氱骇鍚庣鈥濊繎浜嗕竴姝ャ€傛湰杞柊澧?`/health/liveness`銆乣/health/readiness`銆乣/health/diagnostics` 涓夌被鍋ュ悍妫€鏌ユ帴鍙ｏ紝淇濈暀鏃?`/health` 鍏煎鏃ф鏌ヨ矾寰勶紱鍚屾椂鍦?`core/app_logging.py` 涓?JSON 鏃ュ織鍥哄寲浜?`event / trace_id / stage / degrade_reason / dependency` 杩欎簺瀛楁锛岃 HTTP 璇锋眰銆丩LM 澶辫触銆佸璁″啓鍏ャ€乵emory 鍛婅鍜?health probe 鏃ュ織閮芥湁浜嗘洿绋冲畾鐨勮涔夈€傛湰杞繕涓?`controllers/health_controller.py` 鍜?`services/container.py` 琛ヤ簡鍋ュ悍妫€鏌ユ嫶鍒嗗悗鐨勬渶灏忓彲缁存姢缁撴瀯锛屽苟鏂板 `tests/test_health_controller.py` 鎶?legacy `/health` 鍜屾柊鎺ュ彛涓€璧峰洖褰掗獙璇併€?
瀹炵幇鏂瑰紡鍙樺寲锛氭湰杞緷鐒舵病鏈夊紩鍏?Prometheus / OpenTelemetry / ELK SDK 绛夋洿閲嶇殑鎶€鏈爤锛屽彧鏄湪鐜版湁杞婚噺鏋舵瀯涓婃妸鈥滄帴鍙ｈ涔?+ 鏃ュ織璇箟鈥濆厛鍥哄寲銆俵iveness` 鐜板湪琛ㄨ揪鈥滆繘绋嬫槸娲荤殑鈥濓紱`readiness` 琛ㄨ揪鈥滄湇鍔″瑰鎻愪緵鑳藉姏鍓嶇殑渚濊禆灏辩华鈥濓紱`diagnostics` 鍒欑户缁壙鎺ョ涓€杞?snapshot 鏄庣粏锛屽洜姝ゅ悗缁帴鍏ョ洃鎺у钩鍙版椂涓嶉渶瑕佸啀鎶妔I 鍜屽簲鐢ㄧ殑璇箟鍏ㄩ儴鎺ㄥ€掗噸鍋氥€?
褰撳墠闄愬埗鍙樺寲锛氶」鐩幇鍦ㄥ湪鍋ュ悍妫€鏌ュ拰鏃ュ織璇箟涓婃洿鍍忎竴涓彲浜ょ粰鍥㈤槦鐨勬湇鍔★紝浣嗚繕娌℃湁杩涘叆鈥滅湡瑙傛祴鈥濋樁娈点€?1) `trace_id` 鐩墠杩樺彧鍦?HTTP 鍏ュ彛鐢熸垚鍜岃褰曪紝杩樻病鍦ㄦ绱㈠拰闂瓟鍐呴儴閾捐矾涓户缁紶閫掞紱2) `readiness` 鐜板湪浠嶇劧鏄熀浜庡悓杩涚▼鎺㈡椿锛岃€屼笉鏄潰鍚戠洃鎺у钩鍙扮殑鍙傛暟鍖栨寚鏍囪緭鍑猴紱3) 鏃ュ織瀛楁铏界劧鍥哄寲浜嗭紝浣嗚繕娌℃帴鍒伴泦涓紡鏃ュ織妫€绱㈠悗绔€?
涓嬩竴姝ュ缓璁細
1. 缁х画鍋氣€滆交閲廡race鈥濓細鎶?`trace_id` 浠?main.py` 鎺ュ叆鍒?`qa/answering.py`銆乣services/` 鍜?audit 锛岃鍗曟 `/chat` 鐨勬绱銆丩LM 鐢熸垚銆丼uard 闄嶇骇銆佸璁¤兘鍦ㄦ棩蹇椾腑涓茶捣鏉ャ€?
2. 鎶?`readiness` 鍜?`diagnostics` 缁х画鎷嗗紑锛歝eadiness` 淇濇寔缁欒皟搴︾郴缁熷仛鏄惁鎺ユ祦鐨勫垽鏂紝`diagnostics` 涓撻棬鐣欑粰浜哄伐鎺掓煡鍜屽紑鍙戣皟璇曘€?
3. 绗笁杞?优先琛?metrics/monitoring`锛屼緥濡?QPS銆丳95銆丩LM 鎴愬姛鐜囥€侀檷绾х巼銆丮ySQL/Redis 鎺㈡椿鎴愬姛鐜囥€乮ndex job 鍫嗙Н鏁帮紝璁╄繖浜涙棩蹇楀瓧娈典笉鍙槸鈥滃啓鍑烘潵浜嗏€濓紝鑰屾槸鈥滅湡鑳界敤鏉ョ湅鈥濄€?

## 2026-06-10 绗竴闃舵宸ョ▼鍖栨敼閫犲熀绾?

鐘舵€侊細閮ㄥ垎瀹屾垚
鏈鍙樻洿锛氬洿缁曗€滃洟闃熷彲寮€鍙?+ 鍩虹鍙笂绾库€濈洰鏍囷紝鍏堣ˉ浜嗕笁涓渶楂樻敹鐩婄殑宸ョ▼鍖栫偣锛?1) 鏂板 `core/app_logging.py`锛屽皢鍚庣鍚姩銆丠TTP 璇锋眰銆丩LM 澶辫触銆佸璁″啓鍏ュ拰 memory 鍛婅浠?`print` 鍗囩骇涓虹粨鏋勫寲 JSON 鏃ュ織锛?2) 鎵╁睍 `/health` 杩斿洖锛屾柊澧?`overall_status + dependencies`锛屽 MySQL銆丷edis銆乮ndex_jobs 鍜屽悜閲忓悗绔仛鏈€灏忎緷璧栨帰娲伙紝鍚屾椂淇濈暀鏃?`memory` 瀛楁鍏煎鐜版湁璋冪敤锛?3) 澧炲己 `.github/workflows/ci.yml`锛屽湪鍘熸湁娴嬭瘯鍓嶅鍔?`python -m compileall` 缂栬瘧闂ㄧ锛岃 CI 鍏堟尅浣忔槑鏄剧殑璇硶/瀵煎叆绾ч敊璇€?
瀹炵幇鏂瑰紡鍙樺寲锛氭湰娆℃病鏈夊紩鍏ュぇ閲忔柊渚濊禆鎴栧ぇ瑙勬ā閲嶆瀯锛岃€屾槸缁х画娌跨敤鐜版湁鍒嗗眰锛屽湪 `main.py` 鎺ヤ簡杞婚噺璇锋眰鏃ュ織涓棿浠讹紝鍦?`services/container.py` 闆嗕腑瀹炵幇渚濊禆鎺㈡椿锛屽苟鐢?`tests/test_health_snapshot.py` 鍜?`tests/test_mysql_pool.py` 琛ラ綈鏈€灏忓洖褰掗獙璇併€傝繖涓€杞晠鎰忔病鏈夌珛鍒诲紩鍏?Prometheus銆乼elemetry銆乺uff/mypy 绛夋洿閲嶇殑宸ュ叿閾撅紝鍏堜紭鍏堢‘淇濃€滀笉鎵撴柇褰撳墠寮€鍙戣妭濂忊€濄€?
褰撳墠闄愬埗鍙樺寲锛氶」鐩幇鍦ㄥ凡缁忔瘮涔嬪墠鏇村儚鈥滃彲浜ょ粰鍥㈤槦鍚堜綔鐨勫悗绔€濓紝浣嗚窛绂荤湡姝ｄ紒涓氱骇杩樻湁涓夊潡纭澶达細1) `/health` 鐩墠杩樻槸鈥滃悓杩涚▼鍐呮帰娲?+ 蹇収鈥濓紝杩樹笉鏄畬鏁寸殑 readiness/liveness/metrics 浣撶郴锛?2) 鏃ュ織宸茶緭鍑烘垚缁撴瀯鍖?JSON锛屼絾杩樻病鏈夋帴鍒伴泦涓紡瑙傛祴鍚庣锛?3) CI 鐩墠鍙槸鍦ㄢ€滄祴璇曞墠鍏堝仛缂栬瘧闂ㄧ鈥濊繖涓按浣嶏紝杩樻病杩涘叆鏇撮珮寮哄害鐨勯潤鎬佹鏌ュ拰澶氱幆澧冮獙璇併€?
涓嬩竴姝ュ缓璁細
1. 绗簩杞厛琛モ€滃彲瑙傛祴钀藉湴鈥濓細涓鸿姹傘€佹绱㈠拰 LLM 璋冪敤琛ヨ冻 trace_id / stage / latency / degrade_reason 鍥哄畾瀛楁锛屽啀鎶婃棩蹇楁帴鍏?Loki / ELK / OpenSearch 涔嬩竴銆?
2. 鎶?`/health` 鎷嗘垚鏇存槑纭殑 `liveness` / `readiness` / `diagnostics`锛屽苟涓?index job 鍫嗙Н銆丩LM 涓嶅彲鐢ㄣ€佸悜閲忓悗绔垏鎹㈠け璐ョ瓑鍦烘櫙琛ヤ竴缁勬祴璇曘€?
3. 绗笁杞啀涓?CI 鍔犵爜锛氬湪涓嶅奖鍝嶅紑鍙戦€熷害鐨勫墠鎻愪笅锛屽紩鍏?ruff / mypy 鎴栭€夋嫨鏇撮€傚悎褰撳墠浠撳簱鐨勯潤鎬侀棬绂佺粍鍚堛€?
## 2026-06-10 第三轮企业级工程化补强

状态：已完成
本次变更：围绕“可观测链路闭环”继续补了三块最值钱的工程化能力。1）把 `trace_id` 从 `main.py` 的 HTTP 中间件一路透传到 `controllers/chat_controller.py`、`services/chat_service.py`、`agent_system/facade.py` 和 `qa/answering.py`，让一次 `/v1/chat` 请求在问答 trace 和 audit 里都能串起来。2）在 `qa/answering.py` 给检索、答案生成、output guard 和整条 `ask` 链路补了阶段日志与轻量 runtime metrics，固定输出 `stage / trace_id / duration_ms / degraded` 等关键信息。3）在 `services/container.py` 的 diagnostics 快照里增加 `runtime_metrics`，并补了 `tests/test_chat_controller.py` 与 `tests/test_answering_safety.py`，验证 trace 透传、audit 记录和运行时指标都真实生效。
实现方式变化：这一轮没有上 Prometheus、OpenTelemetry 之类更重的基础设施，而是先在现有 FastAPI + 本地进程内状态上补齐最小可用观测面；这样能先把“请求能追踪、关键阶段有耗时、诊断接口能看当前运行态”三件事做实，同时不引入新的部署负担。
当前限制变化：项目现在已经具备轻量级 trace 和 runtime metrics，但仍然不是完整的分布式观测系统；`trace_id` 目前主要覆盖单进程内的 HTTP -> QA -> audit 链路，runtime metrics 也是进程内累计快照，重启会清空，也还没有接入外部指标后端、告警平台或长期存储。
下一步建议：
1. 如果继续往企业级靠拢，优先把 `runtime_metrics` 接到 Prometheus 暴露口，至少补上 QPS、P95、降级率和依赖探活成功率。
2. 把 `trace_id` 继续透传到索引任务、知识记忆写入和外部 LLM 调用，避免观测链路只停在 chat 主链。
3. 在 CI 里继续加一层静态质量门禁，例如 `ruff` 或 `mypy`，让“能跑”和“更稳”之间少留空档。
